# Optional. If not set, the app runs fully rule-based (no LLM calls).
ANTHROPIC_API_KEY=

# Optional. Defaults to claude-sonnet-5 if not set.
CLAUDE_MODEL=claude-sonnet-5

# Optional. Local CPU-friendly sentence-transformers model for RAG embeddings.
EMBEDDING_MODEL=all-MiniLM-L6-v2
